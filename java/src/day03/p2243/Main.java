package day03.p2243;

public class Main {
    



    // public int queryCount(int node, int left, int right, int target) {
    //     if (left == right) {
    //         return left;
    //     } else {
    //         int mid = (left + right) / 2;
    //         if (tree[node * 2] >= target) {
    //             return queryCount (2 * node, left, mid ,target);
    //         } else {
    //             target -= tree[2 * node];
    //             if (tree[node * 2 + 1] >= target) {
    //                 return queryCount(2 * node + 1, mid + 1, right, target);
    //             } else {
    //                 return 0;
    //             }
    //         }
    //     }
    // }
}